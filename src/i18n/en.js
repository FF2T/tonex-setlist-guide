// src/i18n/en.js — English dictionary.
//
// Empty stub. À remplir au fur et à mesure que les composants UI sont
// wrappés via t('key') / tFormat / tPlural. Structure mirror de fr.js,
// keys kebab-case namespaced par feature.
//
// Tant qu'une clé manque ici, l'helper t() fallback automatiquement
// sur fr.js puis sur le fallback inline (la string FR hardcoded
// d'origine), donc l'app reste fonctionnelle même 100% vide.

const en = {
  // À remplir Phase 7.37+ (wrapping screens).
};

export default en;
