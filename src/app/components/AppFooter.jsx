// src/app/components/AppFooter.jsx — Phase 1, étape 5.
// Espace vide en bas de page pour ne pas masquer le contenu sous la
// nav fixe mobile. Extrait verbatim.

import React from 'react';

function AppFooter({syncStatus}){
  return <div style={{height:60}}></div>;
}

export default AppFooter;
